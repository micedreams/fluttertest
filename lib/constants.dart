import 'package:flutter/material.dart';

class Constants {
  static const googleSignInClientId =
      "799472674812-c9u7v9fh59595qpvucq4fkltg87ke0b5.apps.googleusercontent.com";
  static const primaryColor = Color.fromARGB(255, 145, 198, 241);
  static const seconderyColor = Color.fromARGB(255, 187, 205, 220);
  static const thirdColour = Color.fromARGB(255, 238, 241, 244);
  static const clearIcon = Icon(Icons.clear);
  static const backIcon = Icon(Icons.arrow_back);
  static const logoutIcon = Icon(Icons.logout);
  static const downArrowIcon = Icon(Icons.arrow_drop_down);
  static const searchIcon = Icon(Icons.search);
  static const sideArrowIcon = Icon(Icons.arrow_right);
  static const columnPadding = EdgeInsets.only(left: 8.0);
  static const firstElementPadding = EdgeInsets.all(4.0);
  static const otherElementsPadding =
      EdgeInsets.only(top: 8.0, left: 4.0, right: 4.0, bottom: 4.0);
}
